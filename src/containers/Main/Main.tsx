import { Header } from "../../components/Header/Header"


export const Main = () => {
  return (
    <main>
        <Header/>
        {
            //inputBar
            //TaskList
            //Show message if task list is empty instead
        }
    </main>
  )
}
